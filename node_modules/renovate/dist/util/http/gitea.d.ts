import type { HttpOptions, HttpRequestOptions, HttpResponse, InternalHttpOptions } from './types';
import { Http } from '.';
export declare const setBaseUrl: (newBaseUrl: string) => void;
export interface GiteaHttpOptions extends HttpOptions {
    paginate?: boolean;
}
export declare class GiteaHttp extends Http<GiteaHttpOptions> {
    constructor(options?: HttpOptions);
    protected request<T>(path: string, options?: InternalHttpOptions & GiteaHttpOptions & HttpRequestOptions<T>): Promise<HttpResponse<T>>;
}
