import type { PackageRule, PackageRuleInputConfig } from '../../config/types';
import { Matcher } from './base';
export declare class PackageNameMatcher extends Matcher {
    matches({ depName, packageName }: PackageRuleInputConfig, packageRule: PackageRule): boolean | null;
    excludes({ depName }: PackageRuleInputConfig, { excludePackageNames }: PackageRule): boolean | null;
}
