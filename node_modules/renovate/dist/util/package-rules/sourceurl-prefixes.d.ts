import type { PackageRule, PackageRuleInputConfig } from '../../config/types';
import { Matcher } from './base';
export declare class SourceUrlPrefixesMatcher extends Matcher {
    matches({ sourceUrl }: PackageRuleInputConfig, { matchSourceUrlPrefixes }: PackageRule): boolean | null;
}
