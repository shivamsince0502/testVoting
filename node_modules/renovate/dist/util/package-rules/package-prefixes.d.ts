import type { PackageRule, PackageRuleInputConfig } from '../../config/types';
import { Matcher } from './base';
export declare class PackagePrefixesMatcher extends Matcher {
    matches({ depName, packageName }: PackageRuleInputConfig, { matchPackagePrefixes }: PackageRule): boolean | null;
    excludes({ depName }: PackageRuleInputConfig, { excludePackagePrefixes }: PackageRule): boolean | null;
}
