import type { PackageRule, PackageRuleInputConfig } from '../../config/types';
import { Matcher } from './base';
export declare class DepPatternsMatcher extends Matcher {
    matches({ depName, updateType }: PackageRuleInputConfig, { matchDepPatterns }: PackageRule): boolean | null;
    excludes({ depName, updateType }: PackageRuleInputConfig, { excludeDepPatterns }: PackageRule): boolean | null;
}
