import type { Category } from '../../../constants';
export { extractPackageFile } from './extract';
export declare const supportedDatasources: "docker"[];
export declare const defaultConfig: {
    fileMatch: string[];
    versioning: string;
};
export declare const categories: Category[];
