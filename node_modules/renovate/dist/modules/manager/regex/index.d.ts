import type { ExtractConfig, PackageFileContent, Result } from '../types';
export declare const defaultConfig: {
    pinDigests: boolean;
};
export declare const supportedDatasources: string[];
export declare function extractPackageFile(content: string, packageFile: string, config: ExtractConfig): Result<PackageFileContent | null>;
