import { Http } from '../../../util/http';
import { Throttle } from '../../../util/http/throttle';
export declare class RubygemsHttp extends Http {
    protected getThrottle(url: string): Throttle | null;
}
